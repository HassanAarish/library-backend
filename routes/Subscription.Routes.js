import express from "express";
// import * as subsController from "../controller/Subscription.Controller.js";
// import verifyToken from "../middlewares/verifyToken.js";

const router = express.Router();

// router.post(
//   "/initiate-subscription",
//   verifyToken,
//   subsController.initiateSubscription
// );

// router.post(
//   "/create-subscription",
//   verifyToken,
//   subsController.createSubscription
// );

// router.get("/my-subscription", verifyToken, subsController.userSubscription);

// router.get(
//   "/canceled-subs",
//   verifyToken,
//   subsController.getCanceledSubscriptions
// );

// router.post("/create-product", verifyToken, subsController.createProduct);

// router.get("/all-products", verifyToken, subsController.getAllProducts);

// router.patch("/edit-products", verifyToken, subsController.editProductPrice);

export default router;
